<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginEnd="@dimen/activity_horizontal_margin_medium"
    android:layout_marginRight="@dimen/activity_horizontal_margin_medium">

    <ImageView
        android:id="@+id/play_arrow_iv"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:background="@drawable/play_arrow"
        android:contentDescription="@string/play_arrow_content_description" />

    <TextView
        android:id="@+id/tvTrailer"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:gravity="start|center_vertical"
        android:ellipsize="end"
        android:singleLine="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Menu"
        app:layout_constraintLeft_toRightOf="@+id/play_arrow_iv"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="trailer"/>

    <View
        android:id="@+id/view_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#dadada"
        app:layout_constraintTop_toBottomOf="@id/play_arrow_iv"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="@dimen/activity_horizontal_margin_medium"
        android:layout_marginEnd="@dimen/activity_horizontal_margin_medium"/>

</android.support.constraint.ConstraintLayout>
